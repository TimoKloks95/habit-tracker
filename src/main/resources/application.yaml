spring:
  application:
    name: habit-tracker
  datasource:
    url: jdbc:mysql://localhost:3306/habittracker
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
